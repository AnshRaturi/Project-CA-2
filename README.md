## How to Run the Project

### Prerequisites
- Docker
- Docker Compose

### Steps
```bash
git clone https://github.com/AnshRaturi/api-rate-limiter.git
cd api-rate-limiter
docker-compose up -d --build
